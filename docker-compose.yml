version: '3.8'

services:
  app:
    build: .
    container_name: retail-copilot
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      - ENV=development
    restart: unless-stopped
